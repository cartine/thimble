name: verify-release

# Rebuild the published release payloads and compare them with the
# checksummed release assets. Runs on every release tag (after the
# release workflow has finished publishing) and on a monthly schedule
# against the latest release as a long-tail integrity check.

on:
  release:
    types: [published]
  schedule:
    # First day of each month at 09:00 UTC.
    - cron: "0 9 1 * *"
  workflow_dispatch:
    inputs:
      version:
        description: "Release tag to verify (e.g. v0.1.0). Leave blank for latest."
        required: false

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Resolve version
        id: ver
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            echo "version=${{ inputs.version }}" >>"$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "release" ]; then
            echo "version=${{ github.event.release.tag_name }}" >>"$GITHUB_OUTPUT"
          else
            api="https://api.github.com/repos/${{ github.repository }}/releases/latest"
            tag="$(curl -fsSL "$api" \
              | sed -n 's/.*"tag_name": *"\([^"]*\)".*/\1/p' | head -n 1)"
            echo "version=$tag" >>"$GITHUB_OUTPUT"
          fi
      - name: Fetch tags
        run: git fetch --tags --force
      - name: Verify release
        env:
          THIMBLE_REPO: ${{ github.repository }}
        run: bash scripts/verify-release.sh "${{ steps.ver.outputs.version }}"
